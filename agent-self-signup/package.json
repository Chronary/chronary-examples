{
  "name": "agent-self-signup",
  "version": "0.0.0",
  "private": true,
  "description": "Chronary example — an AI agent registers itself via OTP, no human signup",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@chronary/sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
