{
  "name": "team-scheduling-bot",
  "version": "0.0.0",
  "private": true,
  "description": "Chronary hero example — iCal subscription + cross-agent availability + multi-agent proposals + outbound feeds, in one script",
  "type": "module",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@chronary/sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
