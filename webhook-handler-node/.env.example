# Required — the secret returned when you created the webhook.
# Shown ONCE in the API response from POST /v1/webhooks; never retrievable later.
CHRONARY_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional — port to listen on (default 3000).
PORT=3000
